package cn.xiaobaihome.xiaobaihelper.mvvm.base

enum class PageState {
    STATE_LOADING,
    STATE_SUCCESS,
    STATE_ERROR,
    STATE_EMPTY
}

enum class State {
    STATE_LOADING,
    STATE_SUCCESS,
    STATE_ERROR,
    STATE_EMPTY
}